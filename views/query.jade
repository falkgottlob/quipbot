h1= title
ul
  - for record in records
    li= record.get("Name")


table(title=rtype)
  tr
   each value,key in records[0]
    th #{key}
  each record in records
     tr
      each value, key in record  
       td  #{value}